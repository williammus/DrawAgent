{
  "key": "summarizer",
  "value": "A professional, scientific diagram in the style of a top-tier conference, flat 2D vector illustration, academic and minimal, white background, crisp grid-aligned layout, high-contrast readable placeholder text, no 3D shadow, no photorealism, no decorative or non-academic imagery. Section 1 (Left): depict the input multivariate time series $X \\in \\mathbb{R}^{T\\times d}$ as a clean long rectangular data block with a simplified embedded waveform, flowing into a clearly separated Forward Process containing Instance Normalization followed immediately by Channel Independence + Patching; preserve strict left-to-right data flow and show per-channel processing as an abstract channel-split arrangement rather than a single monolithic encoder. Section 2 (Middle): place the central Dual Attention Contrastive Structure as the visual focal point inside a double-bordered container, split into two symmetric parallel branches originating from the same patchified input, not from different samples; the upper branch is Patch-wise Attention / Patch-wise Representation modeling patch-to-patch dependencies, the lower branch is In-patch Attention / In-patch Representation modeling point-to-point dependencies inside each patch, both rendered as standardized geometric modules with matching sizes and aligned spacing. Explicitly include multi-scale parallel patch-size paths feeding both branches, using stacked or parallel abstract scale blocks labeled with placeholder patch-size markers, then route each branch through an Upsampling step and a compact multi-scale aggregation node so that the final aligned outputs are the final patch-wise representation $N$ and final in-patch representation $P$; emphasize that comparison occurs only after alignment, and that the learned target is a permutation-invariant representation. Section 3 (Right): show Representation Discrepancy as a distinct training-time module using bidirectional KL divergence with stop-gradient, visually separated from inference, with dashed or dash-dot dependency lines from $N$ and $P$ into an amber-colored Contrastive Loss $L$ node, and explicitly omit any reconstruction decoder, reconstruction target, or reconstruction loss; in the upper-right inference path, connect $N$ and $P$ to Anomaly Criterion, compute bidirectional KL as the anomaly score, then pass the score through Threshold $\\delta$ to produce the final Anomaly Label $Y$, with the score-to-threshold-to-label sequence строго aligned in a single clean row. Include a small legend area with high-contrast text and concise mapping for color and line semantics: deep blue for core modules, cyan-blue and teal for parallel representation branches, amber for loss/constraint, dark red for anomaly output, solid arrows for forward/inference flow, dashed arrows for training dependency, dash-dot arrows for discrepancy/constraint relations, and simple geometric shape semantics for input, modules, tensors, core innovation container, and special operator nodes. Maintain an information-dense but uncluttered KDD-style composition, all modules snapped to an invisible grid, balanced white space, no line crossings through the core, symmetric branch placement, clear typography, and polished academic visual hierarchy."
}