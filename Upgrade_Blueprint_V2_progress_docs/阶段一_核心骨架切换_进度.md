# 阶段一进度：核心骨架切换

## 1. 当前结论

阶段一已完成，可作为阶段二“后端最小闭环打通”的基线版本继续推进。

本阶段已完成的目标：

- 后端 graph 主干已切换为双节点循环骨架
- 主控决策协议已从旧的节点路由模式切换到 tool call 模式
- 运行时已切换为“启动注册工具、执行时动态创建业务 executor”
- 主控层已具备原生 tool-calling 调用能力
- orchestrator 运行时默认 prompt 已切到正式 `v2`
- 阶段一相关后端测试与 lint 已通过

## 2. 交接说明

当前代码基线的职责边界如下：

- 阶段一只完成骨架、契约和运行时切换
- 阶段二再继续完成业务 tool 的正式迁移、后端完整最小闭环与接口协议切换
- 前端双输入区、上传入口移除不属于本阶段交付

对接时请按以下原则继续：

- 不要再回到旧的多业务节点 graph 方案
- 不要继续扩散 `selected_nodes` 之类旧主控协议
- 不要在主控层直接生成任何业务 `payload_*`
- 不要把 `ask_clarification` 重新实现为 graph 独立节点

## 3. 当前遗留与后续关注点

以下内容已明确留给阶段二及后续阶段：

- 五个业务 tool 的正式统一 executor 契约收口
- `source_text / user_feedback` 的外部 HTTP 协议显式双字段切换
- 业务 prompt 资产除 orchestrator 外的正式 `v2.md` 全量切换
- 审查失败后的最小回滚路径完善
- 前端双输入区与默认主路径去附件化

继续实施时要特别注意：

- 外部 chat API 当前仍保留薄兼容层，不应把它误认为最终协议
- 控制语义与业务语义已经拆分，阶段二不要再次耦合
- 运行时 prompt 版本策略已开始切换，后续新增 prompt 资产需与当前版本策略保持一致

## 4. 验证记录

本阶段完成时已执行并通过：

- `D:\ProgramData\Anaconda3\envs\DrawAgent\python.exe backend\manage.py test`
- `D:\ProgramData\Anaconda3\envs\DrawAgent\python.exe backend\manage.py lint`

## 5. 环境与操作注意事项

- 当前终端默认 `python` 不是本项目后端验证应使用的解释器
- 后端验证与运行请显式使用 `D:\ProgramData\Anaconda3\envs\DrawAgent\python.exe`
- 修改文件时尽量分块提交，避免一次性大补丁导致补丁失败
- 仓库中存在与本轮无关的历史目录和其他变更，后续阶段继续开发时不要误清理、误回退
