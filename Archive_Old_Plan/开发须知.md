# 该md记录开发时遇到的一些疑难问，比如环境事实等
## 当前终端默认 `python` 指向 Anaconda 基座环境；后续后端验证与运行应显式使用 `conda run -n DrawAgent ...`
## 当前机器上 `conda run -n DrawAgent python` 仍落到旧的 Python 3.7；实际可用环境是 `D:\ProgramData\Anaconda3\envs\DrawAgent\python.exe`
## 当前环境修改文件时尽量不要出现大补丁，否则可能出现✘ Failed to apply patch，需要大补丁时分块编辑。